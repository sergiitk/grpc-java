apiVersion: skaffold/v2beta9
kind: Config
build:
  tagPolicy:
    sha256: {}
#    dateTime:
#      format: 2006-01-02_15-04-05_MST
#      timezone: UTC
  # defines where to find the code at build time and where to push the resulting image
  artifacts:
    - image: gcr.io/grpc-testing/xds-k8s-test-server-java
      docker:
        dockerfile: test-server.Dockerfile
    - image: gcr.io/grpc-testing/xds-k8s-test-client-java
      docker:
        dockerfile: test-client.Dockerfile
  local: {}

#profiles:
## use the cloudbuild profile to build images using Google Cloud Build
#- name: cloudbuild
#  build:
#    googleCloudBuild: {}
## use the dockerfile profile to build images using Docker instead of Jib
#- name: dockerfile
#  build:
#    artifacts:
#      - image: java-hello-world
